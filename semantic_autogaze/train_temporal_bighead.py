"""STUB — see `train_distill_bighead.py` for context. Architecture:
`bighead.TemporalBigHead`. Observed not to beat spatial-only bighead."""

# pragma: no cover
raise NotImplementedError("STUB — reconstruct from cluster when available.")
