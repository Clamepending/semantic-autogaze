"""STUB — runs CLIPSeg to precompute per-query per-patch segmentation targets.
See wandb runs `byxh0gxl`, `u0ug3mle`. Args: video_dir, num_frames=16,
queries_per_video=12, rounds=3, autogaze_model=nvidia/AutoGaze.
Writes `clipseg_cache/` used by all downstream training scripts.
"""

# pragma: no cover
raise NotImplementedError("STUB — reconstruct from cluster when available.")
